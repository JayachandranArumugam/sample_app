<!DOCTYPE html>
<html>
<head>
	<title><%= title %></title>
	<%= render 'layouts/stylesheets' %>
</head>
<body>
	<div class="container"> 
	<%= render 'layouts/header' %> 
      </div>	
      <div id="content" class="round"> 
        <%= yield %>
         <a href = '/users/new' class="signup_button round">Sign up now!</a>	
      </div>
      <%= render 'layouts/footer' %>
    </div>
</body>
</html>